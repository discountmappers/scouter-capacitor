import React from 'react';
import { TextSearch } from '../components/TextSearch';

export const ProfileContainer = (props: any) => {
    return (
        <>
            <div>Soon to be Profile Page</div>
        </>
    );
};
